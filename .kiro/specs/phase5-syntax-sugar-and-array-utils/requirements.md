# Requirements Document

## Project Description (Input)
Phase 5: 構文糖衣と配列ユーティリティの実装。Sugu言語の書きやすさを向上させるため、以下の3つのStepを実装する。Step 1: インクリメント/デクリメント演算子(++/--)と複合代入演算子(+=, -=, *=, /=, %=)の追加。Step 2: 配列・文字列の負のインデックス(arr[-1])とスライス操作(arr[1:3])の実装。Step 3: contains()関数とconcat()関数の追加による配列ユーティリティの強化。

## Requirements
<!-- Will be generated in /kiro:spec-requirements phase -->
